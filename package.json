{
  "name": "fm-card-form",
  "version": "1.0.0",
  "type": "module",
  "description": "front end mentor challenge : interactive card detail form",
  "main": "index.js",
  "scripts": {
    "sass": "sass --watch ./sass/main.scss:./styles/style.css",
    "test": "NODE_OPTIONS=--experimental-vm-modules npx jest",
    "test:watch": "NODE_OPTIONS=--experimental-vm-modules npx jest --watch --verbose",
    "test:cov": "NODE_OPTIONS=--experimental-vm-modules npx jest jest --coverage"
  },
  "keywords": [
    "frontend",
    "front",
    "end",
    "challenge",
    "html",
    "sass",
    "form"
  ],
  "author": "Carlos",
  "license": "ISC",
  "devDependencies": {
    "jest": "^29.0.1",
    "jest-environment-jsdom": "^29.0.1",
    "sass": "^1.54.5",
    "@types/jest": "^28.1.8"
  }
}
